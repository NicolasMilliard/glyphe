export function SiteFooter() {
  return (
    <footer className="border-border border-t">
      <div className="text-muted-foreground mx-auto flex w-full max-w-6xl flex-col gap-2 px-(--page-gutter) py-6 text-sm sm:flex-row sm:items-center sm:justify-between">
        <p>Terminal-inspired motion primitives for the web.</p>
        <p className="font-mono text-xs uppercase">CSS-first · Ownable code</p>
      </div>
    </footer>
  );
}
